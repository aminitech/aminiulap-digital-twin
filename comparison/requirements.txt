# Dependencies for the comparison studies C1, C2 and C3.
#
#   python3 -m venv venv && venv/bin/pip install -r comparison/requirements.txt
#
# All three studies solve real Sionna RT scenes, so sionna-rt is required even though
# C1's challenger and C2's TR 38.901 model are pure closed-form NumPy.

sionna-rt==2.0.1     # the version pinned by ulap-scope and cited in the paper
matplotlib>=3.8
numpy>=1.26
scipy>=1.11
